import Foundation

/// Represents HTTP methods used for network requests.
enum HTTPMethod: String {
  /// The HTTP GET method, used for retrieving data from the server.
  case get = "GET"

  /// The HTTP POST method, used for sending data to the server.
  case post = "POST"
}
